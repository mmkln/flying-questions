{
  "name": "flying-questions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --port 4174",
    "test": "node --test src/*.test.js",
    "predeploy": "npm test && npm run build",
    "deploy": "gh-pages -d dist --nojekyll -m \"Deploy site\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.3.6"
  }
}
